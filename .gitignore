target/
node_modules/
*.log
instances.json
accounts.json
settings.json
*.db
.DS_Store

# Kollegen in-game mod (gradle build artifacts)
kollegen-mod/build/
kollegen-mod/.gradle/
kollegen-mod/.idea/

# Tauri updater signing secret key (minisign box format)
minisign.key

# Server SSH keys (never commit)
Kollegenserver
Kollegenserver.pub

# Backend server runtime data (friends/presence store)
server/data/

# Reference / inspiration material (not part of the client or mod)
Inspiration/
